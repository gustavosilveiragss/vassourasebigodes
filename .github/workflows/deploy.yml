name: Deploy to GitHub Pages

on:
  push:
    branches: [master, dev]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout master
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Checkout dev into ./dev
        uses: actions/checkout@v4
        with:
          ref: dev
          path: dev

      - name: Cache bust scripts in index.html
        run: |
          sed -i -E 's#(<script src=")([^":]+\.js)(")#\1\2?v=${{ github.sha }}\3#g' index.html
          sed -i -E 's#(<script src=")([^":]+\.js)(")#\1\2?v=${{ github.sha }}\3#g' dev/index.html
          grep '<script' index.html
          grep '<script' dev/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
